#!/usr/bin/env bash
# Launcher CLI para el cuentagotas de Quickshell (SUPER + SHIFT + P)

REAL_PATH="$(readlink -f "${BASH_SOURCE[0]}")"
SCRIPT_DIR="$(cd "$(dirname "$REAL_PATH")" && pwd)"

SEARCH_SCRIPTS=(
    "$SCRIPT_DIR/../scripts/pick_color.sh"
    "$HOME/Documentos/MrDemonc-SHELL/scripts/pick_color.sh"
    "/usr/share/mrdemonc-shell/scripts/pick_color.sh"
)

TARGET_SCRIPT=""
for s in "${SEARCH_SCRIPTS[@]}"; do
    if [ -f "$s" ]; then
        TARGET_SCRIPT="$s"
        break
    fi
done

if [ -z "$TARGET_SCRIPT" ]; then
    echo "Error: No se encontró pick_color.sh" >&2
    exit 1
fi

exec bash "$TARGET_SCRIPT" "$@"
