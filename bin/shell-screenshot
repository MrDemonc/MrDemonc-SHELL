#!/usr/bin/env bash
# Launcher para la herramienta de captura de pantalla

REAL_PATH="$(readlink -f "${BASH_SOURCE[0]}")"
SCRIPT_DIR="$(cd "$(dirname "$REAL_PATH")" && pwd)"

SEARCH_SCRIPTS=(
    "$SCRIPT_DIR/../scripts/take_screenshot.sh"
    "$HOME/Documentos/MrDemonc-SHELL/scripts/take_screenshot.sh"
    "/usr/share/mrdemonc-shell/scripts/take_screenshot.sh"
)

TARGET_SCRIPT=""
for s in "${SEARCH_SCRIPTS[@]}"; do
    if [ -f "$s" ]; then
        TARGET_SCRIPT="$s"
        break
    fi
done

if [ -z "$TARGET_SCRIPT" ]; then
    echo "Error: No se encontró take_screenshot.sh" >&2
    exit 1
fi

exec bash "$TARGET_SCRIPT" "$@"
